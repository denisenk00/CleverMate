server:
  port: 8083
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
spring:
  data:
    mongodb:
      host: #localhost
      port: #27017
      database: messages
      username:
      password:
  rabbitmq:
    host: localhost
    port: 5672
    username:
    password:
auth0:
  issuer: #https://dev-t1tm1qtjpt05vucu.eu.auth0.com/
  audience: #telegram-openapi-api
  issuer-signing-key: